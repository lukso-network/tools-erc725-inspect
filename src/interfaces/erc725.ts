// Used to put UNKNOWN and to deal with missing types in erc725.js
export interface Erc725JsonSchemaAll {
  name: string;
  key: string;
  keyType: string;
  valueContent: string;
  valueType: string;
}
